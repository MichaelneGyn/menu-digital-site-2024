# Database
DATABASE_URL="file:./dev.db"

# NextAuth Configuration (porta padrão: 3001)
NEXTAUTH_URL="http://localhost:3001"
# IMPORTANTE: Gere um secret seguro usando: openssl rand -base64 32
NEXTAUTH_SECRET="your-secret-key-here-generate-a-secure-one"

# SMTP Configuration (opcional para recuperação de senha)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password
SMTP_FROM=your-email@gmail.com

# AWS Configuration (opcional - usar local storage em desenvolvimento)
AWS_PROFILE=your-aws-profile
AWS_REGION=us-west-2
AWS_BUCKET_NAME=your-bucket-name
AWS_FOLDER_PREFIX=your-folder-prefix/
AWS_ACCESS_KEY_ID=your-access-key-id
AWS_SECRET_ACCESS_KEY=your-secret-access-key
# Optional: custom S3 compatible endpoint (e.g., MinIO, Cloudflare R2)
AWS_S3_ENDPOINT=https://your-endpoint.example.com

# Environment
NODE_ENV=development

# 🔒 SEGURANÇA - API Keys (IMPORTANTE!)
# Gere uma chave forte para operações administrativas
# Exemplo: usar https://www.uuidgenerator.net/ ou openssl rand -hex 32
ADMIN_API_KEY="your-super-secret-admin-key-change-this-immediately"

# Supabase (para storage e realtime)
NEXT_PUBLIC_SUPABASE_URL="https://your-project.supabase.co"
NEXT_PUBLIC_SUPABASE_ANON_KEY="your-anon-key"